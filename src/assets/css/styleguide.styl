// Orlo Wang

// ----- COLOR PALETTE -----
$mainFontColor = #393939
$mainBgColor = #f2f0ed
$sipcBgColor = #fbf4e7
$subBgColor = #f2f0ed
$subFrontBgColor = #909090
$IOSborderColor = #e6e4e1

$themeColor = #0ead87
$therdFontColor = #979799


$mainColor          = #f39800
$matchColor         = #5ccc29
$defaultBgColor     = #ffffff
$defaultBorderColor = #e6e4e1
$defaultFontColor   = #393939
$subFontColor       = #909090


// ----- FONTBOOK -----
$normalFont       = 1.16667rem        // 14px
$smallerFont      = 1rem              // 12px
$minFont          = .83333rem         // 10px
$largeFont        = 1.33333rem        // 16px
$largeX3Font      = 1.5rem            // 18px
$largeX4Font      = 1.66667rem        // 20px

$largeX9Font      = 2.5rem            // 30px


// ----- STYLE SIZE -----
$defaultRadius    = .33333rem         // 4px 
$defaultGap       = .66667rem         // 8px

// ----- STYLE BLOCK -----
.ellipsis { 
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.normalBoxShow {
  box-shadow: 0 1px 4px 0 rgba(0, 0, 0, .14);
}

$iconSize = 16rem

// fm skeleton
$fmTopHeight = 44rem
$fmLeftWidth = 180rem

// usr area
$usrAreaPaddingTop = 40rem
$usrImgSize = 96rem
$usrImgBorderSize = 4rem

$switchSize = 24rem
$uploadViewSize = 96rem

:export{
  usrImgSize: $usrImgSize;
  usrImgBorderSize: $usrImgBorderSize;
  switchSize: $switchSize;
  uploadViewSize: $uploadViewSize;
  mainBgColor: $mainBgColor;
  sipcBgColor: $sipcBgColor;
}

// menu stylesheet
$menuListHeight = 32rem

// login
$loginWidth = 320rem;


// ==============================================================
// 去掉移动端可点击元素在点击是的半透明背景
tap0hightlight{-webkit-tap-highlight-color: rgba(0,0,0,0)}
